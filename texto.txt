Commandos de git:
ls /-a
cd 
pwd 
mkdir 
git init
code . 
.git commit -m " "
.git status -s (M= modificado/ ?? = Todavia no fue agregado)
gitignore es para ignorar archivos o carpetas
git log --oneline
git diff 
git diff --staged
git checkout -b (para crear una rama)
git checkout para moverse entre ramas
git push 'para guardar todo'
git push -u origin ramaC(la rama puede ser otra)